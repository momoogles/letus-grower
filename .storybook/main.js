module.exports = {
  stories: ['../blocks/**/*.stories.mdx', '../blocks/**/*.stories.@(ts|tsx)'],
  addons: [
    '@storybook/addon-links',
    '@storybook/addon-essentials',
    '@storybook/addon-viewport',
  ],
}
